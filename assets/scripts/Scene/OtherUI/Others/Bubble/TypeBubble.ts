import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

export enum TYPE_BUBBLE {
    TOP_LEFT,
    TOP_MID,
    TOP_RIGHT,
    BOTTOM_LEFT,
    BOTTOM_MID,
    BOTTOM_RIGHT
}

